# 발행자 관리용 별도 데이터베이스 설정
ISSUER_DB_HOST=localhost
ISSUER_DB_PORT=5432
ISSUER_DB_NAME=coupon_issuers
ISSUER_DB_USER=postgres
ISSUER_DB_PASSWORD=password

# 클라우드 DB 사용 여부 (Railway 등)
USE_CLOUD_ISSUER_DB=false

# 클라우드 DB URL (Railway 등에서 사용)
ISSUER_DATABASE_URL=

# JWT 설정
JWT_SECRET_KEY=your-secret-key-here
JWT_ALGORITHM=HS256
